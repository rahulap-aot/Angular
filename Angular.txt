ng generate component roles - for creating component roles

[(ngModel)]="firstName"  two way data binding
<p>{{ selectedState }}</p>   interplocation
[type]="inputType" property binding


to change port number  - ng serve --port 4209
event binding - functions


directive => Structural , attribute
            ngIf, ngFor || ngClass , ngStyle
